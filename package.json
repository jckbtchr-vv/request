{
  "name": "vv-request",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "build:with-db": "prisma generate && prisma migrate deploy && nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^5.10.0",
    "nuxt": "^3.17.4",
    "vue": "^3.5.16"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.14.0",
    "prisma": "^5.10.0",
    "typescript": "^5.8.3"
  }
}
